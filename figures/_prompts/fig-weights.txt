Horizontal diverging bar chart centered on x=0. Y-axis lists six feature names, sorted top-to-bottom by descending absolute weight. Each bar printed with the exact numeric label at its tip and a horizontal whisker of half-length equal to the std. X-axis label 'Logistic-regression weight', scaled -1.5 to +3.5, with a bold vertical line at x=0. Clean white background, sans-serif labels.

DATA (render the bars with these values exactly; do NOT add or remove bars; do NOT change numbers):
- feature=sbert_rel: weight = +3.09, std = 0.31, color=teal, direction=extends right
- feature=bm25: weight = +1.76, std = 0.13, color=teal, direction=extends right
- feature=tfidf: weight = +0.64, std = 0.18, color=teal, direction=extends right
- feature=entity_overlap: weight = +0.62, std = 0.18, color=teal, direction=extends right
- feature=log_len: weight = -0.98, std = 0.37, color=coral, direction=extends left
- feature=consistency: weight = -0.88, std = 0.10, color=coral, direction=extends left

AXES:
- x: 'Logistic-regression weight', range -1.5..+3.5, ticks at -1.0, 0.0, 1.0, 2.0, 3.0
- y: categorical, 6 ticks labelled top-to-bottom: sbert_rel, bm25, tfidf, entity_overlap, log_len, consistency

LABELS (render verbatim):
- Logistic-regression weight
- sbert_rel
- bm25
- tfidf
- entity_overlap
- log_len
- consistency
- +3.09
- +1.76
- +0.64
- +0.62
- -0.98
- -0.88

LEGEND (enumerate every color / line style on the figure):
- Teal bar: positive weight (signal contributes toward classifying a paragraph as gold)
- Coral bar: negative weight (signal is learned as an anti-signal)
- Horizontal black whisker on each bar: plus-minus one standard deviation across 3 seeds
- Bold vertical black line at x=0: weight-zero reference
